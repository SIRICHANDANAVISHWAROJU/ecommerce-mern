const Footer = () => (
  <footer className="footer">
    <div className="container">
      © {new Date().getFullYear()} ShopMERN — Built with the MERN stack.
    </div>
  </footer>
);

export default Footer;
